import { ESLintUtils } from '@typescript-eslint/utils';
export type ExtraRuleDocs = {
    recommended: 'strict' | 'error' | 'warn';
};
export declare const createRule: <Options extends readonly unknown[], MessageIds extends string>({ meta, name, ...rule }: Readonly<ESLintUtils.RuleWithMetaAndName<Options, MessageIds, ExtraRuleDocs>>) => ESLintUtils.RuleModule<MessageIds, Options, ExtraRuleDocs, ESLintUtils.RuleListener>;
