providers = ["python"]

[phases.setup]
nixPkgs = ["python3", "gcc"] # Added gcc just in case compilation is needed

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
